---
name: quanliucheng-card-agent
description: |
  全流程知识卡片智能体：输入任意主题，生成「制作全流程」风格的科普知识卡片。
  左侧/通栏扁平风插画（浅绿背景、卡通简约）+ 分步骤的图标+文字讲解，讲清它是怎么来的。
  由扣子(Coze)工作流「全流程的风格 / 制作全流程」转换而来。
  支持 OpenAI 兼容的 LLM 和图模型，零依赖 Node 服务器，双击启动.bat 即用。
---

# 全流程知识卡片 · 智能体

## 概述

输入一个主题（如「绿茶」「豆腐」「纸张」），生成一张**「制作全流程」风格的科普知识卡片**：扁平风插画（浅绿背景、卡通简约）+ 分步骤的图标 + 文字讲解，把「它是怎么从原料到成品的」讲得清清楚楚。由扣子(Coze)工作流「全流程的风格 / 制作全流程」转换而来。

## 工作流程

1. 用户输入主题
2. LLM 生成全流程卡片（标题、简介、分步骤的图标+名称+说明、完整绘画 Prompt）
3. （可选）调用图模型生成扁平风插画（浅绿背景、卡通简约、步骤依次排列）
4. 拼装自包含 HTML 卡片（浅绿卡通主题，含插画 + 步骤列表 + 可展开 Prompt）
5. 落盘到 output/ + 前端预览 + 下载

## 启动方式

```bash
# 双击 启动.bat（自动探测 Node 运行时）
# 或命令行：
node server.js
```

默认端口 8790，被占用自动 +1。

## 配置

编辑 `.env`（复制 `.env.example`）：

```
LLM_BASE_URL=https://api.deepseek.com/v1
LLM_API_KEY=sk-xxx
LLM_MODEL=deepseek-chat
IMG_BASE_URL=        # 可选
IMG_API_KEY=
IMG_MODEL=
IMG_SIZE=1792x1024   # 横版更适合多步骤横向排布
```

## Coze 工作流映射

| Coze 节点 | 本技能对应 |
|-----------|-----------|
| 开始（输入主题） | 前端输入框 |
| 大模型（制作全流程 systemPrompt） | `QUANLIUCHENG_SYSTEM` 常量 + `callQuanliuchengLLM()` |
| 图像生成（字节系独占，2304×1728） | `genImage()` 调用通用图模型（默认横版 1792×1024） |
| 结束 | `buildViewer()` 拼装展示页 |

## 内置兜底

- 未配置图模型时，生成「步骤卡片」：图标 + 文字步骤 + 完整 Prompt，可直接复制到豆包 / 即梦 / 通义万相绘图
- 图模型失败时，同样降级为步骤卡片
