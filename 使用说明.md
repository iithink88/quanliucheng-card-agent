# 全流程知识卡片 · 智能体

输入一个主题，生成一张**「制作全流程」风格的科普知识卡片**：扁平风插画（浅绿背景、卡通简约）+ 分步骤的图标 + 文字讲解，把「它是怎么从原料到成品的」讲得清清楚楚。

## 来源

由扣子(Coze)工作流「全流程的风格 / 制作全流程」转换而来。原工作流的「制作全流程」大模型 systemPrompt 已精炼为 `QUANLIUCHENG_SYSTEM` 常量，保留「扁平风插画、浅绿背景、卡通简约、分步骤依次排列、每步名称+配图、文字清晰」的核心结构，并扩展为结构化 JSON（标题 / 简介 / 步骤 / 绘画 Prompt），方便拼装成知识卡片。

## 快速开始

1. 确保已安装 [Node.js](https://nodejs.org)（LTS 版，勾选 Add to PATH）
2. 双击 `启动.bat`，浏览器自动打开 `http://127.0.0.1:8790`
3. 点击右上角 ⚙ 设置：
   - 选择 LLM 服务商（DeepSeek / 通义千问 / 智谱 / Kimi / OpenAI 等），填入 API Key
   - （可选）配置图像模型，直接生成扁平风插画
4. 输入主题（如「绿茶」「豆腐」「纸张」），选择出图方式，点击「开始生成」

## 不填 Key 也能用

未配置图像模型时，系统会生成一个步骤卡片，包含：
- 主题标题与简介
- 分步骤的图标 + 名称 + 说明
- 可直接用于文生图模型的完整 Prompt

你可以复制 Prompt 到豆包、即梦、通义万相等工具继续生成图片。

## 功能

- **全流程拆解**：LLM 把主题拆成 3~8 个有序步骤，每步配 emoji 图标 + 一句话说明
- **AI 插画**：调用图模型生成扁平风、浅绿背景、卡通简约的流程插画（横版更适合多步骤）
- **文案模式**：生成步骤卡片 + Prompt，方便二次创作
- **自包含导出**：生成的 HTML 单文件，离线可看、可打印、可转发

## 文件结构

```
全流程知识卡片智能体/
├── 启动.bat          # 主启动器（自动探测 Node）
├── 启动.vbs          # 备用启动器
├── 修复bat关联.reg    # 修复 .bat 文件关联
├── server.js         # 零依赖 Node 后端
├── index.html        # 前端页面
├── css/styles.css    # 浅绿卡通主题样式
├── js/app.js         # 前端逻辑
├── .env.example      # 配置模板（不含真实 Key）
├── 使用说明.md       # 本文件
├── SKILL.md          # 技能元信息
├── output/           # 生成的卡片存放目录
└── 启动日志.txt      # 运行日志
```
